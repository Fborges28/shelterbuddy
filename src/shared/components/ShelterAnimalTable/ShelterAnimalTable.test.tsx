import React from 'react';
import { render, screen } from '@testing-library/react';

describe('ShelterAnimalTable Unit Tests', () => {
    it.todo("Must render input search");
    it.todo("Must render pagination");
    it.todo("Must render table content");
    it.todo("Must render table content after search");
    it.todo("Must render table feedback content after search with no results");
    it.todo("Must render table feedback content after paginate and search");
    it.todo("Must render table feedback for no content");
    it.todo("Must render table feedback for API error");
})