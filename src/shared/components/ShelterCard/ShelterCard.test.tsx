import React from 'react';
import { render, screen } from '@testing-library/react';

describe('ShelterCard Unit Tests', () => {
    it.todo("Must render card with default info");
    it.todo("Card must be acessible");
})